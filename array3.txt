<?php
    
    $arr = array(5, 1, 1);
    
    echo "Input the number of elements to be stored in the array :3";
    $count_values = array_count_values($arr);
    var_dump($count_values); 
    
    print_r($arr);
    
    echo 'Total number of duplicate elements found in the array is : ' . count()$count_values;
?>